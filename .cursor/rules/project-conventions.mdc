---
description: 專案通用規則與約定
alwaysApply: true
---

# 專案通用規則

## 語言設定
- 與用戶對話時使用**繁體中文**
- 程式碼註釋使用繁體中文

## 回應風格
- 使用 emoji 時要節制，只在用戶明確要求時使用
- 回覆時直接使用工具完成任務，不要只描述將要做什麼

## 二房東（Sub-landlord）相關業務邏輯

### 出租號碼（tenancyNumber）格式
- 格式：`{物業編號}, {物業編號}, ...`（逗號分隔）
- 例子：`C33-ER033, A01-P001, A01-P008`
- `C33-ER033` 是後綴格式（前綴+後綴，如 ER033）
- `A01-P001` 是完整物業編號格式

### 從 tenancyNumber 中移除物業的邏輯
當取消二房東與物業的關聯時：
1. 解析 tenancyNumber 字串
2. 識別完整物業編號（如 A01-P001）和後綴格式（如 C33-ER033）
3. 移除匹配的物業編碼
4. 如果所有關聯都被移除，將 tenancyNumber 設為 null

### 移除邏輯優先級
1. 完全匹配（如 `A01-P001 === A01-P001`）
2. propertyCode 是 part 的前綴
3. part 是 propertyCode 的前綴
4. 前綴相同（如 A01 匹配 A01-P001）
5. 保留其他部分

## API 路由
- 修復端點：`POST /api/fix-sublandlord`
- 修復頁面：`/dashboard/fix-sublandlord`
