'use client';

interface ThemeToggleProps {
    className?: string;
    showLabel?: boolean;
}

export default function ThemeToggle({ className = '', showLabel = false }: ThemeToggleProps) {
    // Theme switcher hidden; site defaults to light mode only.
    return null;
}
